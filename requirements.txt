fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0

# Postgres driver — only needed if DATABASE_URL points at Postgres (e.g. Neon
# or Render Postgres) instead of the SQLite default. Safe to leave installed
# either way.
psycopg[binary]>=3.1,<4.0
